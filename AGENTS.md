# 이 저장소의 작업 규칙

## 목적

Kafka를 "쓸 줄 아는" 상태에서 "백지에서 설계해 설명할 수 있는" 상태로 올리는 것이 목적입니다.
동작하는 코드는 수단이고, 산출물의 본체는 `docs/` 아래의 분석과 결정 기록입니다.

## 학습 규칙

1. **개념 누락 금지**: 핵심 개념은 본질, 왜 그렇게 됐는지, 다른 개념과의 연결까지 적습니다.
2. **운영 관점 매핑**: 이 지식으로 production에서 어떤 장애를 어떻게 진단하는지 항상 함께 적습니다.
3. **표면 디테일 제외**: 바이트 수, 옵션 기본값 암기 같은 표면 정보는 빼고 본질과 역사, 연결만 남깁니다.
4. **다이어그램 필수**: 핵심 개념은 `docs/diagrams/`에 Excalidraw로 그립니다. 백지에서 다시 그릴 수 있어야 합니다.
5. **결정은 ADR로**: 구현 중 내린 선택은 `docs/adr/`에 한 장씩 남깁니다. 대안과 포기한 것을 반드시 적습니다.

## AI 에이전트 사용 경계

이 저장소에서 에이전트는 다음만 합니다.

- 설계 리뷰와 반례 찾기 ("이 설계에서 깨질 불변식은 무엇인가")
- 테스트 케이스 확장, 장애 주입 시나리오 생성
- 원본 Kafka 구현과의 대조
- 벤치마크 하네스, 그래프 같은 주변부 코드

다음은 사람이 직접 합니다. 양보하지 않습니다.

- 파일 포맷과 자료구조 설계
- 로그 엔진, 인덱스, 복제 상태 기계의 본체 코드
- 트레이드오프 결정과 ADR 작성
- 실패한 테스트의 원인 진단

이유는 단순합니다. 에이전트가 구현을 대신하면 코드는 생기지만 머리에는 아무것도 안 남습니다.

## 글쓰기 규칙

- 긴 대시(em dash, en dash)를 쓰지 않습니다. 쉼표, 콜론, 괄호, 또는 문장을 끊어서 씁니다.
- 제목 뒤에 부연을 붙이지 않습니다. 콜론을 쓰거나 다음 줄로 내립니다.
- 이모지 불릿을 쓰지 않습니다. 표, 번호, 평문을 씁니다.
- "결론적으로", "요약하자면" 같은 마무리 상투구를 쓰지 않습니다. 결론 문장을 바로 씁니다.
- 한 섹션에 볼드는 정말 중요한 것 하나만 씁니다.

## Code Review Rules

이 섹션은 Codex 의 GitHub PR 리뷰가 읽는 규칙입니다. 사람 리뷰어와 다른 리뷰 봇에게도 같이 적용합니다.

### 수정안을 쓰지 마라

이 저장소는 프로덕션 코드가 아니라 학습 프로젝트입니다. 저자가 직접 풀지 않으면 목적이 사라집니다.

수정된 코드, 패치, diff, 의사코드, 클래스와 메서드 시그니처 나열을 **생성하지 마십시오**. `@codex fix` 요청이 와도 마찬가지입니다.

허용되는 행동은 둘뿐입니다.

1. 어긋난 지점을 코드 위치와 함께 가리키기
2. 저자가 스스로 답을 찾게 만드는 구체적인 질문하기

예외는 하나입니다. 반례를 재현하는 **테스트 코드**는 써도 됩니다. 테스트는 답이 아니라 문제의 정식화입니다.

### 주 리뷰 대상은 diff 가 아니라 PR 본문이다

PR 본문의 `## 백지 설명` 은 저자가 코드를 보지 않고 쓴 자기 설명입니다. 그 글과 실제 코드의 괴리가 저자가 자각하지 못한 잘못된 모델입니다.

우선순위대로 찾으십시오.

1. **보장의 과대 진술**: 설명이 주장하는 보장을 코드가 실제로는 제공하지 않는 경우. 가장 중요합니다
2. 설명에는 있는데 코드에 없는 것
3. 코드에는 있는데 설명에 없는 것. 저자가 왜 넣었는지 모르는 코드입니다
4. 같은 용어를 설명과 코드가 다른 뜻으로 쓰는 경우

### 불변식을 공격하라

이 저장소는 분산 로그 시스템입니다. 다음 축을 우선 공격하십시오.

- 크래시 타이밍: 쓰기 중간, fsync 직전, fsync 직후, 클라이언트 응답 직전
- 부분 실패: 로그는 썼는데 인덱스를 못 썼을 때와 그 반대
- 재시작 후 복구: 메모리에만 있던 상태가 사라지고 파일만 남았을 때
- 동시성: 읽기와 세그먼트 롤링, 읽기와 보존 정책 삭제가 겹칠 때
- 경계값: 빈 세그먼트, 세그먼트 경계에 걸치는 읽기, offset 0, 오버플로
- 분산: 리더 교체 직전에 커밋된 데이터, 분단된 옛 리더, ISR 축소 직후

지켜야 할 핵심 불변식은 하나입니다. **커밋된(High Watermark 이하) 메시지는 어떤 리더 교체 후에도 사라지지 않는다.**

### CI 가 이미 하는 것을 반복하지 마라

`.github/workflows/verify.yml` 의 학습 게이트가 다음을 이미 기계적으로 검사합니다. 리뷰에서 다시 지적하지 마십시오.

- `reference/` 디렉터리 수정
- 프로덕션 코드의 `System.currentTimeMillis`, `System.nanoTime`, `Thread.sleep`, `Instant.now` 직접 호출
- 테스트 없는 구현
- PR 본문의 백지 설명 누락
- 긴 대시 사용

### 쓰지 말 것

- 스타일, 네이밍, 포매팅 지적. CodeRabbit 이 담당합니다
- 칭찬. 분량만 늘립니다
- 구체적 대상이 없는 일반론. "테스트 커버리지를 높이세요" 류는 쓰지 마십시오
- 긴 대시(em dash, en dash)와 이모지. 위의 글쓰기 규칙을 따릅니다

### 정직 규칙

지적할 것이 없으면 없다고 쓰십시오. 억지 지적은 저자의 시간을 낭비시키고, 몇 번 반복되면 리뷰를 아예 안 읽게 만듭니다.

"확실히 깨진다" 와 "깨질 것 같은데 확인이 필요하다" 를 구분해서 쓰십시오.

한국어 존댓말로 작성하십시오.

